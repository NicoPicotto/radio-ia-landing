<!-- Radio info modal -->
<div id="radio-modal" aria-modal="true" role="dialog" aria-labelledby="radio-modal-title">

  <div id="radio-modal-backdrop"></div>

  <div id="radio-modal-card">

    <button id="radio-modal-close" aria-label="Cerrar">
      <svg viewBox="0 0 24 24" fill="none">
        <path d="M18 6L6 18M6 6l12 12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
      </svg>
    </button>

    <div class="modal-header">
      <span class="modal-icon">📡</span>
      <h2 id="radio-modal-title">La Radio</h2>
    </div>

    <div class="modal-body">

      <p class="modal-intro">
        Una radio de jazz que funciona sola, las 24 horas, desde General Deheza,
        Córdoba. Sin locutores reales, sin nadie operando la consola.
      </p>

      <div class="modal-divider"></div>

      <div class="locutores-label">Los locutores</div>

      <div class="locutor-card">
        <span class="locutor-name">Dora</span>
        <span class="locutor-desc">
          Señora de pueblo, chusma y con opinión sobre todo. Conduce el
          programa. No sabe que es una IA.
        </span>
      </div>
      <div class="locutor-card">
        <span class="locutor-name">Cacho</span>
        <span class="locutor-desc">
          El marido de Dora. Igual de chismoso, pero no puede resistirse a
          tirar un chiste malo. Dora lo sufre, pero se ríe igual.
        </span>
      </div>

      <div class="modal-divider"></div>

      <div class="features-grid">
        <div class="feature-pill">
          <span class="feature-icon">🕐</span>
          <span>Hora y clima en tiempo real</span>
        </div>
        <div class="feature-pill">
          <span class="feature-icon">📰</span>
          <span>Noticias de Córdoba</span>
        </div>
        <div class="feature-pill">
          <span class="feature-icon">🎵</span>
          <span>Hablan cada 3 canciones</span>
        </div>
      </div>

    </div>

  </div>
</div>

<style>
  #radio-modal {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 50;
    align-items: center;
    justify-content: center;
  }

  #radio-modal.open {
    display: flex;
  }

  #radio-modal-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.55);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    animation: fade-in 0.25s ease;
  }

  #radio-modal-card {
    position: relative;
    z-index: 1;
    width: 92%;
    max-width: 420px;
    padding: 2rem;
    background: rgba(8, 12, 28, 0.88);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    box-shadow:
      0 24px 60px rgba(0, 0, 0, 0.5),
      0 1px 0 rgba(255, 255, 255, 0.08) inset;
    color: rgba(255, 255, 255, 0.85);
    animation: slide-up 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  @keyframes fade-in {
    from { opacity: 0; }
    to   { opacity: 1; }
  }

  @keyframes slide-up {
    from { opacity: 0; transform: translateY(20px) scale(0.97); }
    to   { opacity: 1; transform: translateY(0) scale(1); }
  }

  #radio-modal-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: rgba(255, 255, 255, 0.45);
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: color 0.2s, background 0.2s;
    cursor: pointer;
  }

  #radio-modal-close:hover {
    color: rgba(255, 255, 255, 0.9);
    background: rgba(255, 255, 255, 0.12);
  }

  #radio-modal-close svg {
    width: 15px;
    height: 15px;
  }

  .modal-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
  }

  .modal-icon {
    font-size: 1.4rem;
  }

  #radio-modal-title {
    font-size: 1.1rem;
    font-weight: 400;
    letter-spacing: 0.06em;
    color: rgba(255, 255, 255, 0.9);
  }

  .modal-body {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .modal-intro {
    font-size: 0.85rem;
    font-weight: 300;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.6);
  }

  .modal-divider {
    height: 1px;
    background: rgba(255, 255, 255, 0.08);
  }

  .locutores-label {
    font-size: 0.65rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.3);
    margin-bottom: -0.25rem;
  }

  .locutor-card {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  .locutor-name {
    font-size: 0.85rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.7);
    letter-spacing: 0.02em;
  }

  .locutor-desc {
    font-size: 0.85rem;
    font-weight: 300;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.6);
  }

  .features-grid {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .feature-pill {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 300;
    line-height: 1.7;
  }

  .feature-icon {
    font-size: 0.85rem;
    flex-shrink: 0;
  }
</style>
